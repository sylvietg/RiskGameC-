
#include "Game.h"


int main(int argc, char **argv) {
	/*Player *p = new Player;
	p->getPDeck()->printCards();
	int ct = 2;
	p->getPDeck()->addCard(new Card("LOL"));
	p->notify();
	p->getPDeck()->addCard(new Card("LOL"));
	p->notify();
	p->getPDeck()->addCard(new Card("LOL"));
	p->notify();
	p->getPDeck()->addCard(new Card("LOL"));
	p->notify();
	p->getPDeck()->addCard(new Card("LOL"));
	p->notify();
	Reinforcement phase(p, &ct);
	phase.reinforce();*/
//	p->notify();
	//p->getPDeck()->printCards();
	
	/*for (int i = 0; i < c.size(); i++)
	{
		std::cout << c.at(i)->getTerritoryName() << ", ";
		std::cout << c.at(i)->getTypeOfArmyStr() << std::endl;
	}
	*/
  Game g;
  std::cout << "The End" << std::endl;
  return 0;
}

//No territory found with this name!
